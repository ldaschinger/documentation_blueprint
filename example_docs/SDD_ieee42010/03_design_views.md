## 3. Design Views

One entry per unit. `Representation` links to where the design content lives
alongside the unit's source; this section holds no design content of its own
by hand — the full content of each linked doc is pulled in at PDF build time
via a second `{.include}` block in `main.md`.

- ID: 001-example_module
- Title: Example Module (I2C sensor reader)
- Viewpoint: Interface, Interaction, Information
- Representation: [`example_sources/example_layer/example_module/doc/example_module.md`](../../example_sources/example_layer/example_module/doc/example_module.md)
- Requirement IDs: <REQ-IDs implemented by this unit, if tracked>

<!-- Repeat one entry per real unit as they're added, and add its path to the
     matching {.include} block in main.md so its content is pulled into the
     PDF too. -->
