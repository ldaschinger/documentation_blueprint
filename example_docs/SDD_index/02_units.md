## Units

One row per unit; each links to the design doc that lives next to its source
— nothing here is duplicated, only linked.

| Layer | Unit | Design Doc |
| ----- | ---- | ---------- |
| example_layer | example_module | [doc](../../example_sources/example_layer/example_module/doc/example_module.md) |

<!-- Add one row per unit as they're implemented -->
