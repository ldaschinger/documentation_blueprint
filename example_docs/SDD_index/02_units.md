## Units

One row per unit, linking to the design doc that lives next to its source.
Nothing here is duplicated by hand — the full content of each linked doc is
pulled into this document at PDF build time via a second `{.include}` block
in `main.md`.

| Layer | Unit | Design Doc |
| ----- | ---- | ---------- |
| example_layer | example_module | [doc](../../example_sources/example_layer/example_module/doc/example_module.md) |

<!-- Add one row per unit as they're implemented, and add its path to the
     matching {.include} block in main.md so its content is pulled into the
     PDF too. -->
